# secret.yaml — sensitive values (dev sample only)
apiVersion: v1
kind: Secret
metadata:
  name: pydbapi-secrets
  namespace: pydbapi
type: Opaque
stringData:
  # Dev sample only. For staging/production use:
  # kubectl create secret generic pydbapi-secrets -n pydbapi --from-literal=...
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "change-me-postgres-password"
  POSTGRES_DB: "pydbapi_db"
  SECRET_KEY: "change-me-secret-key"
  FIRST_SUPERUSER: "admin@example.com"
  FIRST_SUPERUSER_PASSWORD: "change-me-superuser-password"
