apiVersion: v1
kind: Service
metadata:
  name: {{ include "pydbapi.fullname" . }}
  labels:
    {{- include "pydbapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pydbapi.selectorLabels" . | nindent 4 }}
